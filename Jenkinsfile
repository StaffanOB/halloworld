pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                echo 'Building the application RELEASE'
            }
        }
        stage('Test') { 
            steps {
                echo 'Running Tests RELEASE'
            }
        }
        stage('Deploy') { 
            steps {
                echo 'Deploying application RELEASE'
            }
        }
    }
}
