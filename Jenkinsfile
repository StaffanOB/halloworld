pipeline {
  agen any
  stages {
    stage("build") {
      steps {
        echo 'Building the Release application'
      }
    }
    stage("test") {
      steps {
        echo 'Running Tests Release'
        
      }
    }
    stage("deploy") {
      steps {
        echo 'Deploying Release application'
        
      }
    }
  }
}
