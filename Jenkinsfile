pipeline {
  agen any
  stages {
    stage("build") {
      steps {
        echo 'Building the application'
      }
    }
    stage("test") {
      steps {
        echo 'Running Tests'
        
      }
    }
    stage("deploy") {
      steps {
        echo 'Deploying application'
        
      }
    }
  }
}
